import { useState } from "react";
import '../styles/search.css'
import ReactModal from "react-modal";

function SearchName({onClose, itemList}){
    const handleClickCancel = () => {
        onClose();
    };

    
}